# BAC-009 QA Handoff

상태: DONE

## 완료

- 구현된 backend operationId와 정적 OpenAPI operationId를 런타임에서 비교하는 회귀 테스트 추가.
- 인증/권한/검증 오류 envelope 런타임 테스트 추가.
- FastAPI request validation 오류를 `VALIDATION_ERROR` envelope로 통일.
- 상담 확정 전 연락처 원문 비노출을 `MeetingResponse` schema 테스트로 고정.

## 발견한 Gap

- `getExhibitor`
- `getBooth`
- `updateBoothStatus`

위 3개는 static OpenAPI에는 있으나 실제 route가 없다. `BAC-011`로 분리했다.

## 검증

- backend ruff 통과.
- backend pytest 통과.
