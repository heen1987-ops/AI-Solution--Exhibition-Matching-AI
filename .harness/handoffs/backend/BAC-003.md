# BAC-003 Handoff

Date: 2026-08-02
Track: BACKEND
Status: DONE

## Completed

Implemented the Favorites API backed by `profile.saved_recommendable`.

Endpoints:

- `GET /api/v1/favorites`
- `POST /api/v1/favorites`
- `DELETE /api/v1/favorites/{saved_recommendable_id}`

## Changed Files

- `backend/app/api/v1/endpoints/favorites.py`
- `backend/app/api/v1/api.py`

## Public Interface

The endpoint response models follow CTR-002:

- `AddFavoriteRequest`
- `FavoriteResponse`
- `FavoriteListResponse`

Behavior:

- Missing profile context returns `AUTHENTICATION_REQUIRED` with the common error
  envelope.
- `GUEST_WEB` returns `PERMISSION_DENIED`.
- Saving checks `exhibition.recommendable.active = true` before insert.
- Duplicate `(profile_id, recommendable_id)` saves return `RESOURCE_CONFLICT`.
- Delete is idempotent and scoped to the current profile.

## DB Changes

No DB schema changes. Uses CTR-006 `profile.saved_recommendable`.

## Verification

- `cd backend && ../.venv/bin/ruff check app/api/v1/dependencies.py app/api/v1/errors.py app/api/v1/endpoints/favorites.py app/api/v1/api.py app/main.py`
- `cd backend && ../.venv/bin/python - <<'PY' ...` confirmed unauthenticated
  `GET /api/v1/favorites` returns `401` with `{error:{...}}`.
- `cd backend && ../.venv/bin/pytest tests/test_health_api.py -q`: 6 passed, 1 skipped.
- `cd backend && ../.venv/bin/pytest -q`: 96 passed, 12 skipped.
- `cd backend && ../.venv/bin/ruff check .`

## Known Limitations

DB-backed endpoint integration tests are not added in this BACKEND handoff
because `backend/tests/**` is QA_SECURITY owned. A QA test request was created.

## Follow-up

QA should add contract/integration coverage for successful list/add/delete,
duplicate save conflict, `GUEST_WEB` 403, missing profile 401, inactive
recommendable 404, and cross-profile delete isolation.
