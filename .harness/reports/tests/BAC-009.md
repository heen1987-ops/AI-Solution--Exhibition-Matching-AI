# BAC-009 Contract Regression Report

Date: 2026-08-03

## Result

PASS with explicit residual gaps.

## Added Tests

- `backend/tests/test_bac_group_contract.py`
  - Implemented operationIds match `.harness/contracts/openapi.yaml`.
  - Static OpenAPI route gaps are explicit and bounded.
  - Auth, permission, manual validation, request validation, and recommendation auth failures use `{error:{code,message,request_id,details}}`.
  - `MeetingResponse` exposes only `meeting_id`, `status`, and `contact_disclosed`.

## Runtime Contract Fixes

- Added explicit FastAPI `operation_id` values for implemented health, system, ontology, profile, guest web, search, recommendation, favorites, buyer, meeting, and admin content approval routes.
- Added `RequestValidationError` handler in `backend/app/main.py` so FastAPI request validation returns `VALIDATION_ERROR` in the common envelope.

## Residual Gaps

Resolved by `BAC-011` on 2026-08-03. Current static OpenAPI runtime
missing/mismatch check is 0.

Historical gaps found by BAC-009:

The static OpenAPI contract still defines these operations without corresponding runtime routes:

- `getExhibitor` - `GET /api/v1/exhibitors/{exhibitor_id}`
- `getBooth` - `GET /api/v1/booths/{booth_id}`
- `updateBoothStatus` - `PATCH /api/v1/admin/booths/{booth_id}/status`

These are tracked as `BAC-011`.

## Verification

- `cd backend && ../.venv/bin/ruff check .`
- `cd backend && ../.venv/bin/pytest -q`
