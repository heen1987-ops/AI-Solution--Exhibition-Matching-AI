# Reports

자동 검증 결과 저장 위치. 각 하위 디렉터리(`lint/`, `tests/`, `security/`, `performance/`, `integration/`)에 실행 시각·태스크 ID를 포함한 파일명으로 결과를 남긴다(예: `lint/2026-08-02_FND-003.txt`). 사람이 손으로 편집하지 않는다 - CI/검증 스크립트만 갱신한다.
