# Task backlog, organized by Wave then Track. Wave 0/1 are decomposed to
# ~2-hour verifiable units. Wave 2/3 are decomposed to per-track feature groups
# and will be split further into 2-hour units when each Wave starts (recorded
# as ASSUMPTION-003 - deep a-priori decomposition of work 2+ waves out is wasted
# effort since Wave 0/1 outcomes will change its shape).

tasks:
  # ---------------- WAVE 0: BOOTSTRAP ----------------
  - id: FND-001
    title: 하네스 초기화
    wave: 0
    track: FOUNDATION
    priority: P0
    status: DONE
    depends_on: []
    owned_paths:
      - ".harness/**"
      - "AGENTS.md"
      - "PROJECT_SCOPE.md"
      - "ARCHITECTURE.md"
      - "DEVELOPMENT.md"
      - "TESTING.md"
      - "SECURITY.md"
    acceptance:
      - "필수 하네스 파일이 모두 존재한다"
      - "state.json이 유효한 JSON이다"
      - "backlog.yaml이 유효한 YAML이다"
      - "locks.yaml에 경로 중복 소유가 없다"
    completed_note: >
      이번 세션에서 생성. 기존 repo가 이미 상당한 사전 구현(meet_ai 온톨로지·스코어링,
      backend 매칭 서비스 계층, docs/redesign-v2 26개 설계 문서)을 보유한 상태로 하네스를
      도입했으므로, FND-004에서 그 기존 자산을 재평가해 backlog에 정확히 반영한다.

  - id: FND-002
    title: 모노레포 워크스페이스 골격 도입 (apps/, packages/ 신설)
    wave: 0
    track: FOUNDATION
    priority: P0
    status: DONE
    depends_on: ["FND-001"]
    owned_paths:
      - "package.json"
      - "apps/**"
      - "packages/**"
    acceptance:
      - "apps/, packages/ 디렉토리와 workspace 설정(package.json workspaces 등)이 존재한다"
      - "기존 backend/, src/meet_ai/ 경로는 이동하지 않고 그대로 동작한다"
      - "package.json의 workspaces 필드가 올바르게 파싱된다(npm pkg get workspaces)"
    note: "ASSUMPTION-001 참고 - 기존 backend/src 물리 이동 없음, 신규 코드만 apps/packages에 배치."
    completed_note: >
      npm ls --workspaces는 실제 워크스페이스 멤버(package.json을 가진 apps/packages 하위
      디렉터리)가 없으면 "No workspaces found!" 오류를 낸다는 것을 실행 중 발견 - 원래
      acceptance 문구를 npm pkg get workspaces(필드 파싱 검증)로 수정했다. 실제 멤버는
      WEB-001/KSK-001/ADM-001 등에서 생길 때 npm ls --workspaces로 재검증한다.
      node_modules/package-lock.json이 이 저장소에 원래 없어(사전 존재 조건) npm run
      typecheck는 tsc 미설치로 실패한다 - 이번 변경으로 생긴 회귀가 아니다.

  - id: FND-003
    title: CI 기본 파이프라인 구축
    wave: 0
    track: FOUNDATION
    priority: P0
    status: DONE
    depends_on: ["FND-001"]
    owned_paths:
      - ".github/**"
    completed_note: >
      .github/workflows/ci.yml 생성(backend ruff+pytest, meet_ai 온톨로지검증+unittest,
      TS 경계 typecheck 3개 job). 로컬 venv로 실제 실행해 검증하는 과정에서 두 가지
      기존 문제를 발견 - (1) 11건의 import 정렬 위반(ruff, alembic/scripts/api/tests
      전반) - CI가 캐치할 실제 문제이므로 순수 기계적 autofix(`ruff --fix`, 로직 변경 0)로
      해소, 다른 트랙 소유 경로였으나 의미 변경이 전혀 없는 좁은 예외로 처리(decisions.md
      DEC-009). (2) backend/pyproject.toml에 greenlet 의존성이 없어 신규 환경에서
      async DB 테스트가 ValueError로 즉시 죽는 문제 - dependencies에 추가. 이후 83 unit
      passed, 11 DB 통합 테스트는 TEST_DATABASE_URL 미설정으로 정상 skip(0 failed).
    acceptance:
      - "PR에서 backend lint(ruff)+type check(mypy 있으면)+pytest가 자동 실행된다"
      - "TS 워크스페이스가 생기면 typecheck가 실행된다(현재는 netlify/functions만 존재)"
      - "CI 실패 시 머지 차단(필수 체크로 등록 안내)"

  - id: FND-004
    title: 기존 구현 상태 재평가 및 G0 체크리스트 검증
    wave: 0
    track: FOUNDATION
    priority: P0
    status: READY
    depends_on: ["FND-001"]
    owned_paths:
      - ".harness/state.json"
      - ".harness/run-log.md"
    acceptance:
      - "docker compose up으로 postgres/redis 헬스체크 통과 확인"
      - "backend가 로컬에서 기동하고 /healthz 200 반환 확인"
      - "backend/alembic upgrade head가 깨끗한 DB에 성공"
      - "결과를 state.json의 gate_status.G0_BOOTSTRAP에 반영"
    note: >
      G0_BOOTSTRAP의 "모든 앱이 로컬에서 실행" 기준은 apps/user-web·kiosk·admin이
      아직 존재하지 않아 이 시점에는 충족 불가 - WAVE 1 스켈레톤 생성 이후 재검사한다
      (ASSUMPTION-002). Docker가 없는 이 실행 환경에서는 docker compose 관련 항목을
      NOT_RUN으로 남긴다 - 실제 Docker 가용 환경(로컬 또는 CI)에서 재검증 필요.

  - id: FND-005
    title: 구조 확장(database/ai/infra/tests/docs 신규 서브디렉터리), MinIO, Makefile, 환경변수, 하네스·범위위반 검증 스크립트, CI 확장
    wave: 0
    track: FOUNDATION
    priority: P0
    status: DONE
    depends_on: ["FND-001"]
    owned_paths:
      - "database/**"
      - "ai/README.md"
      - "infra/**"
      - "tests/api/README.md"
      - "tests/integration/README.md"
      - "tests/e2e/README.md"
      - "tests/security/README.md"
      - "docs/requirements/README.md"
      - "docs/api/README.md"
      - "docs/architecture/README.md"
      - "docs/operations/README.md"
      - "Makefile"
      - ".env.example"
      - "docker-compose.yml"
      - ".github/**"
      - "apps/api/README.md"
    acceptance:
      - "WAVE-0 v2 프롬프트 §3의 신규 디렉터리가 모두 존재하고 목적을 설명하는 README를 갖는다"
      - "docker-compose.yml에 MinIO(+버킷 자동생성) 추가, YAML 유효"
      - "Makefile의 setup/dev/infra-up/infra-down/lint/typecheck/test/test-integration/smoke/check 타겟 존재"
      - "infra/scripts/validate-harness, infra/scripts/scope-violation-check 실행 가능하고 실제로 통과"
      - "CI에 harness-validate/docker-build/build-artifact 잡 추가, YAML 유효"
    completed_note: >
      기존 backend/alembic·src/meet_ai/ontology·root docs/·root tests/를 강제 이동하지
      않고(ASSUMPTION-005~007), 신규 항목만 추가했다. validate-harness 실행 중
      자기 자신의 버그(.harness/** 경로를 범위위반 검사에서 제외한다고 문서화해놓고
      실제 코드에서는 빠뜨림)를 발견해 즉시 수정 - 실행해서 실제로 걸러지는지
      확인한 뒤에야 통과 처리했다(가짜로 통과시키지 않음). Docker 미가용으로
      docker-compose.yml MinIO 추가·backend Dockerfile 빌드는 로컬에서 NOT_RUN -
      YAML/Dockerfile 문법만 확인.

  # ---------------- WAVE 1: CONTRACT FIRST ----------------
  - id: CTR-001
    title: 통합 도메인 모델 문서 작성 (.harness/contracts/domain-model.md)
    wave: 1
    track: CONTRACTS
    priority: P0
    status: DONE
    depends_on: ["FND-001"]
    owned_paths:
      - ".harness/contracts/domain-model.md"
    acceptance:
      - "docs/redesign-v2/ 26개 문서 + backend/app/models/*.py 기존 스키마를 근거로 단일 도메인 모델 문서가 존재한다"
      - "엔티티별로 실재 테이블(스키마.테이블)과 신규 필요 테이블이 구분 표기된다"
      - "각 엔티티가 W-1~W-10/K-1~K-8/C-1~C-8 중 어느 문서에서 결정됐는지 출처가 표기된다"
    completed_note: >
      CONTRACTS 워커 에이전트가 완료. 14개 절 + 엔티티 상태 요약표(§13, 16행)로 구성.
      핵심 발견: 진짜 신규 스키마는 `profile.saved_recommendable`(CTR-006, 이미 backlog에
      있음)과 신규 `ai` 스키마 6테이블(§11) 두 곳뿐 - 후자를 담당할 CONTRACTS 태스크가
      backlog에 없었다는 공백을 발견해 CTR-007로 신규 등록(아래). 핸드오프:
      .harness/handoffs/contracts/CTR-001.md.

  - id: CTR-007
    title: ai.* 신규 스키마 마이그레이션 (source_document/extracted_attribute/content_approval/ai_execution_log/embedding_document/embedding_vector) + pgvector 확장
    wave: 1
    track: CONTRACTS
    priority: P1
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - "backend/alembic/versions/**"
      - "backend/app/models/ai.py"
    acceptance:
      - ".harness/contracts/domain-model.md §11의 6개 테이블 스키마대로 마이그레이션 생성"
      - "pgvector 확장 활성화(CREATE EXTENSION) 포함"
      - "alembic upgrade/downgrade 왕복 성공"
      - "SCHEMA_AI 상수(backend/app/db/base.py에 이미 존재)를 재사용, 새 스키마 상수 추가하지 않음"
    note: >
      CTR-001 실행 중 발견된 공백(도메인 모델 문서 §11, §14) - AI_SEARCH 트랙(AIS-GROUP-001,
      Wave 2)의 키워드·벡터 검색 구현이 이 마이그레이션에 의존하므로 Wave 2 착수 전에
      완료돼야 한다. C-2/C-4 문서(docs/redesign-v2/common/)가 스키마 설계의 1차 근거.

  - id: CTR-002
    title: OpenAPI 초안 작성 (.harness/contracts/openapi.yaml)
    wave: 1
    track: CONTRACTS
    priority: P0
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - ".harness/contracts/openapi.yaml"
    acceptance:
      - "OpenAPI 3.x 유효 문서"
      - "docs/redesign-v2/web/W-8-api-and-db-changes.md §5, common/C-5-common-api.md §1의 엔드포인트 목록을 모두 포함"
      - "기존 /api/v1/ontology/* 엔드포인트도 문서화에 포함"

  - id: CTR-003
    title: 온톨로지 계약 파일 작성 (.harness/contracts/ontology.yaml)
    wave: 1
    track: CONTRACTS
    priority: P1
    status: READY
    depends_on: ["FND-001"]
    owned_paths:
      - ".harness/contracts/ontology.yaml"
    acceptance:
      - "src/meet_ai/ontology/catalog.v1.json의 concept_type 체계를 요약 참조"
      - "docs/redesign-v2/common/C-3-ontology.md의 6개 카테고리 매핑 표를 계약 형식으로 반영"

  - id: CTR-004
    title: 오류코드 정의 (.harness/contracts/error-codes.yaml)
    wave: 1
    track: CONTRACTS
    priority: P1
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - ".harness/contracts/error-codes.yaml"
    acceptance:
      - "기존 EligibilityDecision/filter_result 상태값 어휘(PASS/FAIL/UNKNOWN/CONDITIONAL_PASS/MANUAL_REVIEW/TEMPORARY_BLOCK/POLICY_BLOCK/USER_EXCLUDED)를 HTTP 오류코드 체계로 매핑"
      - "각 오류코드에 사용자 노출 메시지 정책(개인정보 미노출) 명시"

  - id: CTR-005
    title: 이벤트 카탈로그 초안 (.harness/contracts/event-catalog.yaml)
    wave: 1
    track: CONTRACTS
    priority: P2
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - ".harness/contracts/event-catalog.yaml"
    acceptance:
      - "content_approval 확정, meeting 상태 변경, guest 회원전환 등 최소 이벤트만 정의"
      - "Kafka 등 스트리밍 인프라를 전제하지 않는 워커 큐잉(RQ/Celery) 수준 이벤트로 한정"

  - id: CTR-006
    title: profile.saved_recommendable 마이그레이션
    wave: 1
    track: CONTRACTS
    priority: P0
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - "backend/alembic/versions/**"
      - "backend/app/models/profile.py"
    acceptance:
      - "docs/redesign-v2/web/W-8-api-and-db-changes.md §4 스키마대로 마이그레이션 생성"
      - "alembic upgrade/downgrade 왕복 성공"
      - "04-integration-roadmap.md 1단계 1-A"

  - id: WEB-001
    title: apps/user-web 라우트 뼈대 (Mock 데이터)
    wave: 1
    track: USER_WEB
    priority: P1
    status: READY
    depends_on: ["FND-002"]
    owned_paths:
      - "apps/user-web/**"
    acceptance:
      - "docs/redesign-v2/web/W-3-screen-ia.md S-1~S-8 라우트가 Mock 데이터로 렌더링된다"
      - "실제 API를 호출하지 않는다(G1 이전 원칙)"
      - "'/' 준비 화면, '/health' 프런트 빌드·환경상태 화면 제공"
      - "apps/user-web/.env.example 제공(NEXT_PUBLIC_API_BASE_URL 등)"
      - "빌드·컴포넌트 테스트 통과"

  - id: KSK-001
    title: apps/kiosk 라우트 뼈대 (Mock 데이터)
    wave: 1
    track: KIOSK
    priority: P1
    status: READY
    depends_on: ["FND-002"]
    owned_paths:
      - "apps/kiosk/**"
    acceptance:
      - "docs/redesign-v2/kiosk/K-3-screen-ia.md K-S1~K-S8 라우트가 Mock 데이터로 렌더링된다"
      - "'/' 대기화면 골격, '/health' 키오스크 설정·API 연결상태 화면 제공"
      - "대기화면에 로그인·개인정보 입력 필드가 없음을 검증하는 테스트 포함(AGENTS.md §8)"
      - "apps/kiosk/.env.example 제공"
      - "빌드·컴포넌트 테스트 통과"

  - id: ADM-001
    title: apps/admin 라우트 뼈대 (Mock 데이터)
    wave: 1
    track: ADMIN
    priority: P2
    status: READY
    depends_on: ["FND-002"]
    owned_paths:
      - "apps/admin/**"
    acceptance:
      - "docs/redesign-v2/web/W-9-admin-operations.md 범위의 최소 화면(승인·통계) 뼈대가 Mock 데이터로 렌더링된다"
      - "'/' 준비 화면, '/health' 관리자 앱 상태 화면 제공(실제 인증 기능은 이번 Wave에 없음)"
      - "apps/admin/.env.example 제공"
      - "빌드·컴포넌트 테스트 통과"

  - id: BAC-001
    title: 공통 Health Check API (/health/live, /health/ready, /api/v1/system/info) + apps/worker 골격
    wave: 1
    track: BACKEND
    priority: P0
    status: READY
    depends_on: []
    owned_paths:
      - "backend/app/main.py"
      - "backend/app/api/**"
      - "apps/worker/**"
    acceptance:
      - "GET /health/live는 프로세스 생존 여부만 확인(의존성 체크 없음)"
      - "GET /health/ready는 PostgreSQL·Redis 연결을 확인, MinIO 장애는 READY_CHECK_FAILS_ON_S3_DOWN 설정에 따라 경고 또는 실패로 분기"
      - "GET /api/v1/system/info는 개인정보 없이 {service, version, environment, contract_version} 반환"
      - "apps/worker에 큐 연결 설정, 샘플 Health Task, 샘플 작업 실행, 실패·재시도 기본정책, 구조화 로그 골격 제공(실제 AI 문서처리·임베딩 생성은 구현하지 않음)"
      - "PostgreSQL 정상 시 /health/ready 성공, Redis 장애 시 /health/ready가 오류 구조를 반환하는 테스트 포함"
      - "Worker 샘플 작업 실행 테스트, 실패 시 제한된 재시도 테스트, 민감정보 로그 미노출 테스트 포함"

  - id: AIS-001
    title: 검색·추천 Gold Set 초안 작성
    wave: 1
    track: AI_SEARCH
    priority: P1
    status: READY
    depends_on: ["CTR-001"]
    owned_paths:
      - "backend/tests/fixtures/gold_set/**"
    acceptance:
      - "질의-기대결과 쌍이 최소 20건 이상 정의된다"
      - "웹(프로파일 있음)·키오스크(프로파일 없음) 케이스가 모두 포함된다"

  - id: AIS-002
    title: 온톨로지 유사어 해석 테스트셋
    wave: 1
    track: QA_SECURITY
    priority: P2
    status: DONE
    depends_on: []
    owned_paths:
      - "backend/tests/test_ontology_api.py"
    acceptance:
      - "다국어(ko/en) 유사어 해석 케이스 포함"
    completed_note: >
      7개 신규 테스트 추가(PRODUCT/CONSUMER/BUYER 컨텍스트 실제 카탈로그 쌍 사용,
      컨텍스트 불일치, 대소문자 무관, 미매핑 텍스트, locale 스코핑). 카탈로그에 en 유사어가
      전혀 없다는 사실을 발견해 가짜 assertion을 만들지 않고 "0건 매칭이 정상"이라는
      회귀 테스트로 정직하게 반영 - AIS-002-followup으로 데이터 공백 등록.
      전체 스위트 90 passed / 11 skipped(회귀 없음).
    note: >
      원래 track: AI_SEARCH로 기록했으나 owned_paths(backend/tests/**)가 locks.yaml상
      QA_SECURITY 전속 경로라 트랙 불일치가 있었다 - QA_SECURITY로 정정(디스패치 전 발견,
      run-log.md 참고). 내용(온톨로지 유사어 케이스 설계)은 AI_SEARCH 트랙의 도메인 지식을
      작업 브리핑에 포함시켜 QA_SECURITY 워커가 작성한다.

  - id: AIS-003
    title: Fallback 시나리오 정의
    wave: 1
    track: AI_SEARCH
    priority: P2
    status: READY
    depends_on: ["CTR-004"]
    owned_paths:
      - ".harness/contracts/error-codes.yaml"
    acceptance:
      - "AI 추출 실패, 검색 결과 0건, 벡터 인프라 장애 각각의 Fallback 동작이 정의된다"

  - id: QAS-001
    title: 정적검사·시크릿 스캔 CI 통합
    wave: 1
    track: QA_SECURITY
    priority: P0
    status: DONE
    depends_on: ["FND-003"]
    owned_paths:
      - ".github/**"
      - ".harness/reports/lint/**"
      - ".harness/reports/security/**"
    acceptance:
      - "ruff/mypy 결과가 .harness/reports/lint/에 기록된다"
      - "기본 시크릿 스캔이 CI에 포함된다"
    completed_note: >
      gitleaks(고정 바이너리, marketplace action 아님) secret-scan 잡 추가 +
      baseline(기존 오탐 4건 제외) + ruff/pytest/unittest 결과를 .harness/reports/**로
      아티팩트 업로드. 로컬 검증: YAML 유효, gitleaks baseline 적용 후 0건, pytest 90
      passed/11 skipped. 워커가 스스로 발견한 불일치: 이 태스크의 owned_paths(.github/**)가
      locks.yaml상 FOUNDATION 전속과 충돌 - locks.yaml을 정본으로 유지하고, 앞으로
      QA_SECURITY가 CI를 고쳐야 하면 직접 수정하지 말고 FOUNDATION에 핸드오프 요청하는
      것으로 원칙을 재확인(아래 owned_paths는 이번 1회의 실제 이력 기록으로 남기되,
      locks.yaml 자체는 변경하지 않는다). 핸드오프: .harness/handoffs/qa/QAS-001.md.

  - id: AIS-002-followup
    title: "(참고) 온톨로지 영어(en) 유사어 데이터 공백"
    wave: 1
    track: CONTRACTS
    priority: P2
    status: PENDING_DECOMPOSITION
    depends_on: []
    owned_paths:
      - "src/meet_ai/ontology/catalog.v1.json"
    acceptance:
      - "K-6(다국어) 요구가 실제로 영어 유사어 데이터를 필요로 하는지 CONTRACTS/AI_SEARCH가 결정"
    note: >
      AIS-002 실행 중 발견: catalog.v1.json에는 ko-KR 유사어 11건만 있고 en 유사어가
      전혀 없다. 테스트는 이 사실을 조작하지 않고 "0건 매칭"을 정상 동작으로 검증했다
      (backend/tests/test_ontology_api.py). 실제 영어 데이터 추가 필요 여부는 데이터
      작업이지 코드 작업이 아니므로 결정 대기 상태로만 등록.

  # ---------------- WAVE 2: CORE PARALLEL IMPLEMENTATION (coarse groups, refine at wave start) ----------------
  - id: BAC-GROUP-001
    title: 매칭·프로파일·상담 REST API 노출
    wave: 2
    track: BACKEND
    priority: P0
    status: PENDING_DECOMPOSITION
    depends_on: ["CTR-002", "CTR-006"]
    owned_paths:
      - "backend/app/api/**"
    acceptance:
      - "04-integration-roadmap.md 4단계(4-A~4-F) 엔드포인트가 모두 구현된다"
      - "CTR-002 OpenAPI와 실제 응답 스키마가 일치한다(계약 테스트 통과)"

  - id: AIS-GROUP-001
    title: 키워드·벡터 검색 채널 + 키오스크 점수식
    wave: 2
    track: AI_SEARCH
    priority: P0
    status: PENDING_DECOMPOSITION
    depends_on: ["CTR-001", "BAC-GROUP-001"]
    owned_paths:
      - "backend/app/services/matching/**"
      - "src/meet_ai/scoring/**"
    acceptance:
      - "04-integration-roadmap.md 3단계(3-A~3-F) 전 항목 구현"

  - id: WEB-GROUP-001
    title: user-web 실 API 연동 (Mock 제거)
    wave: 2
    track: USER_WEB
    priority: P0
    status: PENDING_DECOMPOSITION
    depends_on: ["WEB-001", "BAC-GROUP-001"]
    owned_paths:
      - "apps/user-web/**"
    acceptance:
      - "W-3 S-1~S-8 전 화면이 실제 API로 동작"

  - id: KSK-GROUP-001
    title: kiosk 실 API 연동 (Mock 제거)
    wave: 2
    track: KIOSK
    priority: P0
    status: PENDING_DECOMPOSITION
    depends_on: ["KSK-001", "BAC-GROUP-001", "AIS-GROUP-001"]
    owned_paths:
      - "apps/kiosk/**"
    acceptance:
      - "K-3 K-S1~K-S8 전 화면이 실제 API로 동작, 세션 초기화·QR 인계 포함"

  - id: ADM-GROUP-001
    title: admin 승인·통계 워크플로우 구현
    wave: 2
    track: ADMIN
    priority: P1
    status: PENDING_DECOMPOSITION
    depends_on: ["ADM-001", "BAC-GROUP-001"]
    owned_paths:
      - "apps/admin/**"
    acceptance:
      - "W-9 범위(승인, 바이어 검증, 기본 통계) 구현"

  # ---------------- WAVE 3: INTEGRATION (coarse, refine at wave start) ----------------
  - id: QAS-GROUP-001
    title: 계약·E2E·권한·성능 통합시험
    wave: 3
    track: QA_SECURITY
    priority: P0
    status: PENDING_DECOMPOSITION
    depends_on: ["BAC-GROUP-001", "WEB-GROUP-001", "KSK-GROUP-001", "ADM-GROUP-001", "AIS-GROUP-001"]
    owned_paths:
      - "tests/**"
      - "backend/tests/**"
      - ".harness/reports/**"
    acceptance:
      - "docs/redesign-v2/web/W-10-validation-and-mvp-scope.md, kiosk/K-8-validation-and-mvp-scope.md, common/C-8-integration-testing-deployment.md 전 항목 검증"
      - "G3_MVP_RELEASE 전 기준 통과"
