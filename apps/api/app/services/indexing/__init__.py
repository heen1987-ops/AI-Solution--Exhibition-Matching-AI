"""BACKEND-INDEXING (WAVE 2D) service package.

See ``app/models/indexing.py`` module docstring for the schema rationale and
``document_builder.py`` / ``service.py`` module docstrings for the two-layer
split (pure document construction vs. DB-touching orchestration).
"""
