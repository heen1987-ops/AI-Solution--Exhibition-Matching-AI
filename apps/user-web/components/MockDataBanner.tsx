export function MockDataBanner() {
  return (
    <p className="rounded-md bg-amber-50 px-3 py-2 text-xs text-amber-800 dark:bg-amber-950 dark:text-amber-200">
      이 화면은 Wave 1 라우트 뼈대(WEB-001)이며 Mock 데이터만 표시합니다. 실제 API 연동은
      G1_CONTRACT_FREEZE 이후 WEB-GROUP-001에서 진행됩니다.
    </p>
  );
}
